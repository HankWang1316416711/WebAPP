/*(function($){
	$.init({
		gestureConfig:{
			tap:true,
			doubletap:true,
			longtap:true,
			swipeleft:true,
			drag:true,
			hold:false,
			release:false
		}

	});
	var colllegenews = document.getElementById('news');

	collegeNews.addEventListener('tap',function(){
		alert('ssss');
	})
}(window.mui = window.mui || {}))
*/